// middleware.ts has been disabled in favor of `app/proxy.ts`.
// The original middleware logic was preserved in `middleware.legacy.ts`.
//
// Keeping this file minimal prevents duplicate/mixed middleware behavior
// while allowing the `app/proxy.ts` implementation to take effect.

// No runtime exports in this file.
